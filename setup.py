# setup.py (in root of credit-risk-model/)
from setuptools import setup, find_packages

setup(
    name='credit_risk_model',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
)